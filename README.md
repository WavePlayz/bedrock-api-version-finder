# min-minecraft-module-finder

Find the minimum @minecraft/* module version your Bedrock Script API code needs. Paste a folder or code — tsc type-checks it against every published version and tells you the oldest one that works.
